# 🛠️ PLAN DE REFACTORIZACIÓN SUGERIDO: ALGARROBO_BASE2

## 🎯 RESUMEN ESTRATÉGICO
- **Enfoque**: Seguridad (P0), Robustez (P1) y Mantenibilidad (P2).
- **Esfuerzo Estimado**: 5-8 días hombre (Incremental).

---

### 1️⃣ QUICK WINS: SEGURIDAD Y INTEGRIDAD (P0)

**🔨 Refactor**: Ofuscación a Ocultamiento de API Key (IA).
**🎯 Objetivo**: Resolver criticidad de `compliance-reviewer`.
**💻 Código (After)**: 
```python
# En Flask (backend)
@app.route('/api/chat', methods=['POST'])
@session_required
def chat():
    # Enviar a ZhipuAI con API_KEY guardada en server .env
    pass
```
**⚠️ Riesgo**: Bajo. (Requiere mover frontend a llamar a `/api/chat`).
**⏱️ Esfuerzo**: S.

**🔨 Refactor**: SRI (Subresource Integrity) Universal.
**🎯 Objetivo**: Evitar inyección de scripts por CDNs comprometidos.
**💻 Código (Before/After)**: 
```html
<!-- Antes -->
<script src="https://cdn.tailwindcss.com"></script>
<!-- Después -->
<script src="https://cdn.tailwindcss.com" integrity="..." crossorigin="anonymous"></script>
```
**⚠️ Riesgo**: Nulo.
**⏱️ Esfuerzo**: S (Automatizable con `refactor_backend.js`).

---

### 2️⃣ MEJORAS DE ROBUSTEZ Y UX (P1)

**🔨 Refactor**: Null-checks en scripts compartidos (`layout.js`, `router.js`).
**🎯 Objetivo**: Eliminar errores de runtime en el dashboard.
**💻 Código (After)**: 
```javascript
const menu = document.getElementById('userMenu');
if (menu) menu.classList.toggle('hidden'); // Blindaje contra nulos
```
**⚠️ Riesgo**: Nulo.
**⏱️ Esfuerzo**: S.

---

### 3️⃣ EVOLUCIÓN E ARQUITECTURA (P2)

**🔨 Refactor**: Migración a ES Modules (ESM).
**🎯 Objetivo**: Mejorar la mantenibilidad y desacoplamiento de scripts globales.
**💻 Código (After)**: 
```javascript
// En index.html
<script type="module" src="./script/main.js"></script>
```
**⚠️ Riesgo**: Medio (Requiere actualizar imports y orden de scripts).
**⏱️ Esfuerzo**: M.

---

**🔨 Refactor**: Componentización de la UI con DocumentFragments.
**🎯 Objetivo**: Optimizar performance de renderizado (Layout Thrashing).
**💻 Código (After)**:
```javascript
const fragment = document.createDocumentFragment();
list.forEach(item => fragment.appendChild(createItemNode(item)));
container.appendChild(fragment);
```
**⚠️ Riesgo**: Bajo.
**⏱️ Esfuerzo**: M.
